# Optional future integration dependencies.
# The graph runner uses LangGraph when installed and a deterministic local
# fallback otherwise so MSYS/Windows environments without native wheels still run.
langgraph==0.0.55
