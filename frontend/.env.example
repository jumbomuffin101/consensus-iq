# FastAPI backend URL used by the Next.js frontend.
# Set this to the deployed backend URL in production.
# Production builds require this value; local development falls back to http://localhost:8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
